# Operations

Operational changes are different from normal code changes because they affect deployment, authentication, email delivery, domains, or secrets. Track them in issues and PRs even when the final change happens in a dashboard.

## Deployment

The site is deployed on Vercel from GitHub.

Required production environment variables:

```txt
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
```

Rules:

- Do not commit `.vercel/`.
- Do not commit `.env.local`.
- Do not put service role keys in Vercel unless a server-only feature explicitly requires it.
- If server-only secrets are added later, document the exact server path that needs them.

Recommended checks after deployment:

```bash
curl -I https://bremen-landing.vercel.app
```

Run a smoke test for:

- `/`
- `/performances`
- `/videos`
- `/photos`
- `/history`
- `/members`
- `/login`

## Supabase Auth

Auth redirects must include every active domain.

Expected redirect categories:

- production site URL
- Vercel preview wildcard
- local development callback

When adding or changing a domain:

1. Update Supabase Auth Site URL if the canonical production domain changes.
2. Add callback URLs for production, preview, and local development.
3. Test signup or password reset end-to-end.
4. Document the result in the issue or PR.

## Email and SMTP

The project uses custom SMTP through Resend.

Do not commit:

- SMTP host password
- Resend API keys
- DKIM/SPF/DMARC private material
- Supabase service role key

When changing email templates:

- Keep the language user-facing, not developer-facing.
- Include enough context for POSTECH mail filters to trust the message.
- Test delivery to a POSTECH address when possible.
- Keep auth links generated by Supabase template variables.

## Supabase Storage

Current image policy:

- Upload public-facing images to Supabase Storage.
- Store the full public URL in `entities.thumbnail_url` or `members.avatar_url`.
- Use only `thumbnail_url` for generic entity display unless a renderer truly needs multiple image roles.

Guardrails:

- Do not make a private bucket public without an ops issue.
- Do not delete buckets through MCP or scripts.
- Do not mass-delete storage objects without a reviewed maintenance plan.

## Public Repository Safety

This repository can stay public if these remain true:

- `.env.local` is ignored.
- `.mcp.json` is ignored.
- `.omc/` is ignored.
- `.vercel/` is ignored.
- Service role keys are never committed.
- SMTP credentials are never committed.
- Supabase RLS remains enabled.

Before making the repo more visible, run:

```bash
git grep -n -E 'SUPABASE_SERVICE_ROLE_KE[Y]=.+|s[b]_secret|s[k]-[A-Za-z0-9]|g[h]p_|github_p[a]t_|BEGIN (RSA|OPENSSH|PRIVATE) KEY' -- . ':!pnpm-lock.yaml'
```

The command should return no real secrets.

## Incident Response

If an agent or human may have changed production data incorrectly:

1. Stop further writes.
2. Record the exact command, tool, and time.
3. Check recent migrations and Supabase logs.
4. Export or snapshot affected rows before trying to fix them.
5. Fix forward with a reviewed migration or maintenance script.
6. Document what happened in the issue or PR.
